# Base URL of the Next.js API (apps/server).
VITE_API_URL=http://localhost:4000

# Supabase project's public keys. These are safe in the browser: they are the
# anon key, constrained by the row level security policies in the migration.
# Used only for Realtime (job progress + live leaderboards).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
